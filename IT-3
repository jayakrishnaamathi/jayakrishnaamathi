import React from 'react';
import { View, Text, Button } from 'react-native';
import tailwind from 'tailwind-rn';

function ServicesScreen({ navigation }) {
    return (
        <View style={tailwind('flex-1 items-center justify-center bg-gray-100')}>
            <Text style={tailwind('text-4xl font-bold text-blue-600 mb-4')}>Our Services</Text>
            <Text style={tailwind('text-lg text-gray-700 mb-2')}>• Custom Software Development</Text>
            <Text style={tailwind('text-lg text-gray-700 mb-2')}>• Web Design & Development</Text>
            <Text style={tailwind('text-lg text-gray-700 mb-2')}>• Mobile App Development</Text>
            <Text style={tailwind('text-lg text-gray-700 mb-2')}>• Cloud Computing Solutions</Text>
            <Button title="Home" onPress={() => navigation.navigate('Home')} />
            <Button title="About Us" onPress={() => navigation.navigate('AboutUs')} />
            <Button title="Portfolio" onPress={() => navigation.navigate('Portfolio')} />
            <Button title="Contact Us" onPress={() => navigation.navigate('ContactUs')} />
        </View>
    );
}

export default ServicesScreen;
